<div class="top">
	<a href="{{ route('uslugs.index') }}" class="btn btn-primary">{{ __('APP.SERVICES') }}</a>
	<a href="{{ route('products') }}" class="btn btn-primary">{{ __('APP.PRODUCTS') }}</a>
</div>